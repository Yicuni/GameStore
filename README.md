一、直接解压项目，从Idea里打开，我用的是Tomcat8.5.90；
二、用Navicat创建一个数据库叫做gamesql，然后把gamesql.sql文件导入到这个数据库里；
三、记得把resources文件夹里的prop.properties里的配置项的参数改成和自己电脑的一样；
四、先运行一下项目，然后把file.zip里的图片解压到运行项目后产生的\GameStore\out\artifacts\GameStore里面，文件夹名称是写死了的，叫做file(直接解压到目标位置就行，不用创建file文件夹)。
五、基本可以了
